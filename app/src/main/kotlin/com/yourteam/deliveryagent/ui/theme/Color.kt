package com.yourteam.deliveryagent.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand palette — logistics / delivery feel ─────────────────────────────────
val DeepOrange       = Color(0xFFE65100)   // primary — energetic, action-oriented
val DeepOrangeLight  = Color(0xFFFF8A65)   // primary container
val Amber            = Color(0xFFFFB300)   // secondary — earnings / money highlights
val AmberLight       = Color(0xFFFFE082)

// ── Neutral surface ───────────────────────────────────────────────────────────
val SurfaceLight     = Color(0xFFFAFAFA)
val SurfaceDark      = Color(0xFF1C1C1E)
val OnSurfaceVariant = Color(0xFF757575)

// ── Status colors ─────────────────────────────────────────────────────────────
val SuccessGreen     = Color(0xFF2E7D32)
val ErrorRed         = Color(0xFFC62828)
